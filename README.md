# Server Log Auditor Pro

A Python CLI tool for automated server log analysis and feature extraction.

## ⚙️ Setup
1. Clone this repository or download `main.py` and `server.log`.
2. Ensure you have Python 3.x installed.

## 🚀 Usage
Run the script from your terminal using:
```bash
python main.py server.log
